---
- hosts: all
  name: Test playbooks
  tasks:
    - name: Debug
      debug:
        var: ansible_env.HOME
    - name: Simple Message
      select_options:
        msg: What?
        variable: chosenPackages
        options:
          - Yksi
          - Kaksi
    - name: Debug
      debug:
        msg: "{{chosenPackages}}"
    - name: Debug
      debug:
        msg: Yksi
      when: "'Yksi' in chosenPackages"

